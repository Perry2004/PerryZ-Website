// for some unknown reason, importing bootstrap from ts does not work properly, so I have to import it from js
// this line is solely for the purpose of importing bootstrap to enable accordion
import * as bootstrap from 'bootstrap'
